% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_hdx_fonts.R
\name{load_hdx_fonts}
\alias{load_hdx_fonts}
\title{Load and use the HDX 2025 redesign fonts}
\usage{
load_hdx_fonts(
  display_family = NULL,
  display_regular = NULL,
  body_family = NULL,
  body_regular = NULL
)
}
\arguments{
\item{display_family}{Character string for the Merriweather family name to
register, used whether the font is downloaded from Google or loaded
locally. If \code{NULL}, defaults to \code{"Merriweather"}, the standard family
name. See "Details" in the \code{\link[sysfonts:font_add]{sysfonts::font_add()}} documentation for
further explanation.}

\item{display_regular}{Path to the font file for the Merriweather regular
font face. If \code{NULL}, defaults to \code{"Merriweather-Regular.ttf"}, the
standard file name downloaded from
\href{https://fonts.google.com/specimen/Merriweather}{Merriweather}. Used
only when no internet connection is available to directly load from
Google.}

\item{body_family}{Character string for the Roboto family name to
register, used whether the font is downloaded from Google or loaded
locally. If \code{NULL}, defaults to \code{"Roboto"}, the standard family name.}

\item{body_regular}{Path to the font file for the Roboto regular font
face. If \code{NULL}, defaults to \code{"Roboto-Regular.ttf"}, the standard file
name downloaded from \href{https://fonts.google.com/specimen/Roboto}{Roboto}.
Used only when no internet connection is available to directly load
from Google.}
}
\value{
Nothing, run for side effect of loading the fonts and activating
showtext.
}
\description{
Simple wrapper for \code{sysfonts::font_add_google()} and
\code{showtext::showtext_auto()} to load the two fonts used in the HDX
website's 2025 redesign,
\href{https://fonts.google.com/specimen/Merriweather}{Merriweather} for display
and heading text, and \href{https://fonts.google.com/specimen/Roboto}{Roboto}
for body text, and specify all plots to automatically use showtext. Use to
load the default font families for \code{\link[=theme_hdx]{theme_hdx()}} and \code{\link[=gghdx]{gghdx()}}.
}
\details{
By default, both fonts are loaded from Google using
\code{sysfonts::font_add_google()}. If an internet connection is unavailable,
then attempts to use locally installed versions of the fonts using
\code{sysfonts::font_add(family, regular)}. If you have the fonts installed but
still receive an error from this function, check the \verb{*_family} and
\verb{*_regular} arguments match your installed fonts.
}
\examples{
library(ggplot2)

p <- ggplot(mtcars) +
  geom_point(
    aes(
      x = mpg,
      y = hp
    )
  ) +
  labs(
    x = "Miles per gallon",
    y = "Horsepower",
    title = "Horsepower relative to miles per gallon"
  )

# font not loaded so an error will be generated
try(p + theme_hdx())

load_hdx_fonts()

p + theme_hdx()

}
\seealso{
\code{\link[=gghdx]{gghdx()}} for automatically running \code{load_hdx_fonts()}, along
with other styling. \code{\link[=load_source_sans_3]{load_source_sans_3()}} to instead load the
original HDX font, kept for backwards compatibility.
}
